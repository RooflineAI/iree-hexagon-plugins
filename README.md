# IREE Hexagon Plugin

This repo contains experimental compiler and runtime plugins for targeting
the Qualcomm Hexagon NPU with IREE. It serves a a temporary staging area for
the prototype code that is intended to be upstreamed into the experimental
folder of the main IREE repo `https://github.com/iree-org/iree/`.

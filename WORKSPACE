# This file is intentionally minimal. All dependencies are declared in
# MODULE.bazel via Bzlmod. An empty WORKSPACE file keeps Bazel from treating
# some other directory further up the filesystem as the workspace root.
workspace(name = "iree_hexagon_plugins")

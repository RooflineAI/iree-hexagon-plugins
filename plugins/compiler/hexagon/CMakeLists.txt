################################################################################
# Autogenerated by third-party/iree/build_tools/bazel_to_cmake/bazel_to_cmake.py from#
# plugins/compiler/hexagon/BUILD.bazel                                         #
#                                                                              #
# Use iree_cmake_extra_content from iree/build_defs.oss.bzl to add arbitrary   #
# CMake-only content.                                                          #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

iree_cc_library(
  NAME
    registration
  HDRS

  SRCS
    "PluginRegistration.cpp"
  DEPS
    iree::compiler::PluginAPI
    iree_hexagon_plugins::plugins::compiler::hexagon::Target::HexagonSession
  ALWAYSLINK
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/..>"
  PUBLIC
)

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###

# Registers this plugin with IREE's CMake compiler-plugin mechanism
# (build_tools/cmake/iree_plugin_register.cmake), discovered via
# plugins/iree_compiler_plugin.cmake (see -DIREE_CMAKE_PLUGIN_PATHS). This
# repo's Bazel build instead lists this plugin in
# build_tools/bazel/default_compiler_plugins.bzl -- the two mechanisms are
# unrelated (third-party/iree/compiler/plugins/BUILD.bazel carries a
# "bazel-to-cmake: skip" marker specifically because CMake plugin
# registration always uses this call instead), so this is added here by hand
# rather than mirrored from the Bazel side.
iree_compiler_register_plugin(
  PLUGIN_ID
    hexagon
  TARGET
    ::registration
)

################################################################################
# Autogenerated by third-party/iree/build_tools/bazel_to_cmake/bazel_to_cmake.py from#
# plugins/runtime/hexagon/dsp/BUILD.bazel                                      #
#                                                                              #
# Use iree_cmake_extra_content from iree/build_defs.oss.bzl to add arbitrary   #
# CMake-only content.                                                          #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

iree_package_ns(_PACKAGE_NS)
add_library(${_PACKAGE_NS}::qurt_lib ALIAS hexagon_sdk::qurt_lib)

if(IREE_HEXAGON_MLIR_DSP_BUILD AND IREE_HEXAGON_MLIR_DSP_BUILD)
iree_cc_library(
  NAME
    hexagon_dsp_skel
  SRCS
    "${CMAKE_CURRENT_BINARY_DIR}/../interface/hexagon_dsp/hexagon_dsp_skel.c"
    "align.h"
    "buffer.c"
    "command_buffer.c"
    "device.c"
    "executable.c"
    "executable.h"
    "import_provider.c"
    "import_provider.h"
    "power_mode.c"
    "power_mode.h"
  COPTS
    "-Wswitch-enum"
  DEPS
    ::qurt_lib
    hexagon_mlir::bin::runtime::runtime_lib
    iree::hal::local::executable_library
    iree_hexagon_plugins::plugins::runtime::hexagon::arm_dsp::hexagon_arm_dsp
    iree_hexagon_plugins::plugins::runtime::hexagon::dsp::pmu::hexagon_pmu_helpers
    iree_hexagon_plugins::plugins::runtime::hexagon::dsp::rt::hexagon_rt_library
    iree_hexagon_plugins::plugins::runtime::hexagon::interface::hexagon_dsp_skel_hdr_lib
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  SHARED
)

iree_package_name(_PACKAGE_NAME)
set_target_properties(${_PACKAGE_NAME}_hexagon_dsp_skel PROPERTIES
  OUTPUT_NAME "hexagon_dsp_skel"
)
endif()

if(IREE_HEXAGON_MLIR_DSP_BUILD AND IREE_HEXAGON_MLIR_DSP_BUILD)
iree_package_name(_PACKAGE_NAME)
if(IREE_ENABLE_RUNTIME_TRACING AND IREE_TRACING_PROVIDER STREQUAL "tracy")
  target_compile_definitions(${_PACKAGE_NAME}_hexagon_dsp_skel PRIVATE IREE_HAL_HEXAGON_ENABLE_PROFILER)
endif()
endif()

if(IREE_HEXAGON_MLIR_DSP_BUILD AND IREE_HEXAGON_MLIR_DSP_BUILD)
iree_package_name(_PACKAGE_NAME)
add_dependencies(${_PACKAGE_NAME}_hexagon_dsp_skel hexagon_dsp_interface_gen)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###

if(IREE_HEXAGON_MLIR_DSP_BUILD AND IREE_HEXAGON_MLIR_DSP_BUILD)
iree_package_name(_PACKAGE_NAME)
# iree_cc_library() compiles all objects with -fvisibility=hidden
# (IREE_DEFAULT_COPTS) and only re-adds -fvisibility=default when the
# *global* BUILD_SHARED_LIBS is ON. This target opts into SHARED
# individually while the project builds with BUILD_SHARED_LIBS=OFF, so that
# override never happens and every symbol -- including the FastRPC-required
# hexagon_dsp_skel_handle_invoke -- ends up hidden, which makes the DSP's
# adsprpc loader fail with "Undefined symbol" at runtime.
if(IREE_SUPPORTS_VISIBILITY_DEFAULT)
  target_compile_options(${_PACKAGE_NAME}_hexagon_dsp_skel.objects PRIVATE
    "-fvisibility=default"
  )
endif()
endif()

if(IREE_HEXAGON_MLIR_DSP_BUILD AND IREE_HEXAGON_MLIR_DSP_BUILD)
iree_package_name(_PACKAGE_NAME)
# hexagon_mlir::bin::runtime::runtime_lib defines several ABI entry points
# only ever resolved via PLT from a separately dlopen'd, JIT-compiled kernel
# .so -- HexagonCAPI.cpp's hexagon_runtime_{alloc,free}_{1d,2d} and
# hexagon_runtime_copy (called only from HexKLAPI.cpp in that same static
# archive) and UserDMA/RuntimeDMA.cc's hexagon_runtime_dma_{start,wait} and
# hexagon_runtime_dma2d_start (called from nowhere else in this repo at
# all). Nothing in this skel's own link graph references any of them, so the
# linker's normal demand-driven archive extraction drops those object files
# entirely; force the linker to retain them explicitly.
target_link_options(${_PACKAGE_NAME}_hexagon_dsp_skel PRIVATE
  "-Wl,-u,hexagon_runtime_alloc_1d"
  "-Wl,-u,hexagon_runtime_alloc_2d"
  "-Wl,-u,hexagon_runtime_free_1d"
  "-Wl,-u,hexagon_runtime_free_2d"
  "-Wl,-u,hexagon_runtime_copy"
  "-Wl,-u,hexagon_runtime_dma_start"
  "-Wl,-u,hexagon_runtime_dma_wait"
  "-Wl,-u,hexagon_runtime_dma2d_start"
)
endif()

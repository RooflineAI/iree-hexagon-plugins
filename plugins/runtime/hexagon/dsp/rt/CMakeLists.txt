################################################################################
# Autogenerated by third-party/iree/build_tools/bazel_to_cmake/bazel_to_cmake.py from#
# plugins/runtime/hexagon/dsp/rt/BUILD.bazel                                   #
#                                                                              #
# Use iree_cmake_extra_content from iree/build_defs.oss.bzl to add arbitrary   #
# CMake-only content.                                                          #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(IREE_HEXAGON_MLIR_DSP_BUILD AND IREE_HEXAGON_MLIR_DSP_BUILD)
iree_cc_library(
  NAME
    hexagon_rt_library
  HDRS
    "dispatch_state.h"
    "executable_library.h"
    "malloc_free.h"
    "memref_copy.h"
    "profiler.h"
    "runtime_state.h"
    "runtime_state_fwd_decl.h"
    "vtcm_pool.h"
  SRCS
    "malloc_free.c"
    "memref_copy.c"
    "profiler.c"
    "vtcm_pool.cpp"
  DEPS
    hexagon_mlir::bin::runtime::runtime_lib
    iree::hal::local::executable_library
    iree_hexagon_plugins::plugins::runtime::hexagon::arm_dsp::hexagon_arm_dsp
    iree_hexagon_plugins::plugins::runtime::hexagon::dsp::pmu::hexagon_pmu_helpers
  ALWAYSLINK
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../..>"
  PUBLIC
)
endif()

if(IREE_HEXAGON_MLIR_DSP_BUILD AND IREE_HEXAGON_MLIR_DSP_BUILD)
iree_package_name(_PACKAGE_NAME)
# ALWAYSLINK: ${_PACKAGE_NAME}_hexagon_rt_library itself is an INTERFACE
# target; the real compiled objects live in its ".objects" twin.
if(IREE_HEXAGON_PLAYGROUND_PROFILER)
  target_compile_definitions(${_PACKAGE_NAME}_hexagon_rt_library.objects PRIVATE IREE_HAL_HEXAGON_ENABLE_PROFILER)
endif()
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###

if(IREE_HEXAGON_MLIR_DSP_BUILD AND IREE_HEXAGON_MLIR_DSP_BUILD)
iree_package_name(_PACKAGE_NAME)
# iree_cc_library()'s ALWAYSLINK branch has the same -fvisibility=hidden bug
# as its SHARED branch: it only re-adds -fvisibility=default when the
# *global* BUILD_SHARED_LIBS is ON, which this project never sets. This
# library defines hexagon_runtime_malloc/free etc., which get statically
# linked into the hexagon_dsp_skel SHARED library; without this, they stay
# hidden in the final .so and the DSP loader fails to resolve them when
# dlopen'ing a compiled kernel that imports them via PLT.
if(IREE_SUPPORTS_VISIBILITY_DEFAULT)
  target_compile_options(${_PACKAGE_NAME}_hexagon_rt_library.objects PRIVATE
    "-fvisibility=default"
  )
endif()
endif()

################################################################################
# Autogenerated by third-party/iree/build_tools/bazel_to_cmake/bazel_to_cmake.py from#
# plugins/runtime/hexagon/dsp/ukernel/hmx/BUILD.bazel                          #
#                                                                              #
# Use iree_cmake_extra_content from iree/build_defs.oss.bzl to add arbitrary   #
# CMake-only content.                                                          #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(IREE_HEXAGON_MLIR_DSP_BUILD AND IREE_HEXAGON_MLIR_DSP_BUILD)
iree_cc_library(
  NAME
    hmx_ukernels
  COPTS
    "-flax-vector-conversions=all"
    "-Xclang=-target-feature"
    "-Xclang=+hmxv79"
  HDRS
    "api.h"
  SRCS
    "hmx.c"
  ALWAYSLINK
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../..>"
  PUBLIC
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###

# iree_cc_library() compiles ALWAYSLINK object libraries with hidden
# visibility unless BUILD_SHARED_LIBS is enabled globally. The HMX entry points
# are resolved by JIT-compiled executables at loader time, so they must remain
# visible from the DSP skeleton shared object.
if(IREE_HEXAGON_MLIR_DSP_BUILD)
  iree_package_name(_PACKAGE_NAME)
  target_compile_options(${_PACKAGE_NAME}_hmx_ukernels.objects PRIVATE
    "-fvisibility=default"
  )
endif()

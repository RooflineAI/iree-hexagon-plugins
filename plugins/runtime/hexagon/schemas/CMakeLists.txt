################################################################################
# Autogenerated by third-party/iree/build_tools/bazel_to_cmake/bazel_to_cmake.py from#
# plugins/runtime/hexagon/schemas/BUILD.bazel                                  #
#                                                                              #
# Use iree_cmake_extra_content from iree/build_defs.oss.bzl to add arbitrary   #
# CMake-only content.                                                          #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

set(IREE_FLATCC_TOOL_BINARY iree-flatcc-cli)
if(IREE_HOST_BIN_DIR)
  set(IREE_FLATCC_TOOL_BINARY "${IREE_HOST_BIN_DIR}/iree-flatcc-cli")
endif()
add_custom_command(
  OUTPUT
    "${CMAKE_CURRENT_BINARY_DIR}/hexagon_executable_def_reader.h"
    "${CMAKE_CURRENT_BINARY_DIR}/hexagon_executable_def_builder.h"
    "${CMAKE_CURRENT_BINARY_DIR}/hexagon_executable_def_verifier.h"
    "${CMAKE_CURRENT_BINARY_DIR}/flatbuffers_common_reader.h"
    "${CMAKE_CURRENT_BINARY_DIR}/flatbuffers_common_builder.h"
  COMMAND
    "${IREE_FLATCC_TOOL_BINARY}"
    "--common"
    "--reader"
    "--builder"
    "--verifier"
    "-o${CMAKE_CURRENT_BINARY_DIR}"
    "${CMAKE_CURRENT_SOURCE_DIR}/hexagon_executable_def.fbs"
  DEPENDS
    "${CMAKE_CURRENT_SOURCE_DIR}/hexagon_executable_def.fbs"
    ${IREE_FLATCC_TOOL_BINARY}
  VERBATIM
)
add_custom_target(hexagon_executable_def_gen_gen DEPENDS
    "${CMAKE_CURRENT_BINARY_DIR}/hexagon_executable_def_reader.h"
    "${CMAKE_CURRENT_BINARY_DIR}/hexagon_executable_def_builder.h"
    "${CMAKE_CURRENT_BINARY_DIR}/hexagon_executable_def_verifier.h"
    "${CMAKE_CURRENT_BINARY_DIR}/flatbuffers_common_reader.h"
    "${CMAKE_CURRENT_BINARY_DIR}/flatbuffers_common_builder.h"
)

iree_cc_library(
  NAME
    hexagon_executable_def_c_fbs
  HDRS
    "flatbuffers_common_builder.h"
    "flatbuffers_common_reader.h"
    "hexagon_executable_def_builder.h"
    "hexagon_executable_def_reader.h"
    "hexagon_executable_def_verifier.h"
  DEPS
    iree::base::internal::flatcc::building
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###

# iree_cc_library()'s generic "generated header" detection does not reliably
# wire up a build-order edge for consumers in other directories.
# flatbuffer_c_library() adds this explicitly, do the same here.
add_dependencies(iree_hexagon_plugins_plugins_runtime_hexagon_schemas_hexagon_executable_def_c_fbs
                  hexagon_executable_def_gen_gen)
